diff --git a/src/main/java/finalforeach/cosmicreach/RuntimeInfo.java b/src/main/java/finalforeach/cosmicreach/RuntimeInfo.java
index 660ea56..6d62455 100644
--- a/src/main/java/finalforeach/cosmicreach/RuntimeInfo.java
+++ b/src/main/java/finalforeach/cosmicreach/RuntimeInfo.java
@@ -4,11 +4,8 @@ import com.badlogic.gdx.Gdx;
 import java.text.DecimalFormat;
 
 public class RuntimeInfo {
-   private static final String osName = System.getProperty("os.name").toLowerCase();
-   public static final boolean isMac = osName.contains("mac");
-   public static final boolean isWindows = osName.contains("windows");
    public static final String version = Gdx.files.internal("build_assets/version.txt").readString();
-   public static final boolean useSharedIndices = !isMac;
+   public static final String osName = System.getProperty("os.name").toLowerCase();
 
    public static String getJavaHeapUseStr() {
       return byteSizeToHumanReadable(Gdx.app.getJavaHeap());
@@ -32,4 +29,12 @@ public class RuntimeInfo {
          }
       }
    }
+
+   public static boolean isMac() {
+      return osName.contains("mac");
+   }
+
+   public static boolean isWindows() {
+      return osName.contains("windows");
+   }
 }
